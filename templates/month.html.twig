{# templates/calendar.html.twig #}
{{ include('system/head.html.twig') }}

<div class="uk-container" >
    <nav class="uk-navbar-container" uk-navbar>
        <div class="uk-navbar-left">
            <ul class="uk-navbar-nav">
                <li><a href="/">Home</a></li>
            </ul>
        </div>
    </nav>
</div>

<h2 class="uk-panel-title">Month: {{ year }} / {{ month }}</h2>
{% for day in days %}
    <a class="uk-button uk-button-primary uk-width-1-6" href="/cal/{{ day.date }}">{{ day.day }}</a>
{% endfor %}
